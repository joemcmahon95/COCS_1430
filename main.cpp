#include <iostream>
#include <fstream>
using namespace std;

int main(){
  
int nameScore =0;  
for (int i=0;i<line.length();i++)
{
  char letter = line[i];
  nameScore += (int) letter - 64;
}
totalscore += nameScore;
